# Instalacão

```sh
yay -S logiops
```

Crie a configuração necessária contida em `logid.cfg`.

```sh
sudo nano /etc/logid.cfg
```

Ative o serviço:

```sh
sudo systemctl start logid
sudo systemctl enable logid
```

E pronto, pode começar a usar o mouse com os atalhos.